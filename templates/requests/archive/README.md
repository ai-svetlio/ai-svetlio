# Archive - Завършени заявки

Завършени заявки се преместват тук от `processed/`.

При завършване на заявка:
1. Промени статуса на "Завършена" в CR файла
2. Попълни секция "Резултат"
3. Обнови REGISTRY.md
4. Премести файла от `processed/` в `archive/`
